- @comments.each do |comment|
  .media
    .media-body
      %h5.media-heading
        = comment.user.email
        %small
          = time_ago_in_words(comment.created_at)
          ago
      %p= comment.body